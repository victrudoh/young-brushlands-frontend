import styled from "styled-components";

export const Wrapper = styled.div`
    input {
        padding: .3rem 3rem;
        border-radius: 8px;
        border-width: 2px;
        box-shadow: 0px 0px 6px 0px var(--accentColor);
    }
`;